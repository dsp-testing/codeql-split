name: Run CodeQL
on:
  pull_request:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
     - name: Initialize CodeQL
       uses: github/codeql-action/init@v1
       with:
        tools: https://github.com/dsp-testing/codeql-split/releases/download/codeql-bundle-20200904/codeql-bundle-linux64-cpp.tar.gz

